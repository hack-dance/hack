name: hack-cli
services:
  deps:
    image: imbios/bun-node:latest
    working_dir: /app
    volumes:
      - ..:/app
      - node_modules:/app/node_modules
    command: bun install
    networks:
      - default

networks:
  hack-dev:
    external: true

volumes:
  node_modules:
